When begining to use the database, be sure to link the dynamic library for Redox to the program.
Simply type the following into a terminal:

LD_LIBRARY_PATH=*path leading to Redox build directory*
export LD_LIBRARY_PATH
